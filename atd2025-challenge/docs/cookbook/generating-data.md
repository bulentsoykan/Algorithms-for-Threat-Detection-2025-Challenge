# Generating Datasets

This script demonstrates how to generate AIS data in the format used
for this challenge. The script downloads raw AIS From its source database
then processes it into a usable format and outputs a csv. To run the script,
copy and paste it into a local file, then type the following in a terminal
or console:

```console
python /path/to/script
```

```python title="generating_data.py"
--8<-- "scripts/data_processing.py"
```